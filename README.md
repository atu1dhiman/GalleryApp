# GalleryApp
This Gallery App in which we are hitting API to load 5000 images at once  in gallery view and provide smooth loading and scrolling using  lazy loading with NSChache and other optional can use pagination if api dataa is not metadata fromate pagination.
